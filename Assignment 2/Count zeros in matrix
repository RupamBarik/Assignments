C
#include <stdio.h>
int countZero(int mat[100][100], int m, int n) {
    int count = 0;
    for (int i = 0; i < m; i++)
        for (int j = 0; j < n; j++)
            if (mat[i][j] == 0)
                count++;
    return count;
}


Python 
def count_zeros(matrix):
    return sum(row.count(0) for row in matrix)
